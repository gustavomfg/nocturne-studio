name: Release · stable

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: Stable tag selected in "Use workflow from" (for example v1.0.0)
        required: true
        type: string
      codex_smoke_run_id:
        description: Successful Codex contract smoke run ID for the exact release commit
        required: true
        type: string

permissions:
  contents: read
  actions: read

concurrency:
  group: stable-release-${{ inputs.release_tag }}
  cancel-in-progress: false

jobs:
  source-gate:
    name: Verify release source and Codex contract
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      GH_TOKEN: ${{ github.token }}
      RELEASE_TAG: ${{ inputs.release_tag }}
      CODEX_SMOKE_RUN_ID: ${{ inputs.codex_smoke_run_id }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.release_tag }}
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - name: Require a stable version and matching tag
        shell: bash
        run: |
          node -e "const v=require('./package.json').version;const tag=process.env.RELEASE_TAG;if(v.includes('-'))throw new Error('Stable signing requires a version without a prerelease suffix: '+v);if(tag !== 'v'+v)throw new Error('Tag '+tag+' does not match package version v'+v)"
          test "$(git rev-parse HEAD)" = "$(git rev-list -n 1 "$RELEASE_TAG")"
      - name: Require a successful Codex smoke for this commit
        shell: bash
        run: |
          expected_sha="$(git rev-parse HEAD)"
          run_sha="$(gh api "repos/$GITHUB_REPOSITORY/actions/runs/$CODEX_SMOKE_RUN_ID" --jq .head_sha)"
          conclusion="$(gh api "repos/$GITHUB_REPOSITORY/actions/runs/$CODEX_SMOKE_RUN_ID" --jq .conclusion)"
          event="$(gh api "repos/$GITHUB_REPOSITORY/actions/runs/$CODEX_SMOKE_RUN_ID" --jq .event)"
          workflow_path="$(gh api "repos/$GITHUB_REPOSITORY/actions/runs/$CODEX_SMOKE_RUN_ID" --jq .path)"
          test "$run_sha" = "$expected_sha"
          test "$conclusion" = success
          test "$event" = workflow_dispatch
          test "$workflow_path" = .github/workflows/codex-contract-smoke.yml
      - run: npm ci
      - run: npm run typecheck
      - run: npm run lint
      - run: npm test
      - run: npm run verify:release-metadata
      - run: npx playwright install --with-deps chromium
      - run: npm run test:renderer

  signed-package:
    name: Sign and verify (${{ matrix.os }})
    needs: source-gate
    environment: stable-release
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    env:
      RELEASE_TAG: ${{ inputs.release_tag }}
      CSC_LINK: ${{ (matrix.os == 'macos-latest' && secrets.MAC_CSC_LINK) || (matrix.os == 'windows-latest' && secrets.WIN_CSC_LINK) || '' }}
      CSC_KEY_PASSWORD: ${{ (matrix.os == 'macos-latest' && secrets.MAC_CSC_KEY_PASSWORD) || (matrix.os == 'windows-latest' && secrets.WIN_CSC_KEY_PASSWORD) || '' }}
      APPLE_ID: ${{ matrix.os == 'macos-latest' && secrets.APPLE_ID || '' }}
      APPLE_APP_SPECIFIC_PASSWORD: ${{ matrix.os == 'macos-latest' && secrets.APPLE_APP_SPECIFIC_PASSWORD || '' }}
      APPLE_TEAM_ID: ${{ matrix.os == 'macos-latest' && secrets.APPLE_TEAM_ID || '' }}
      GPG_PRIVATE_KEY: ${{ matrix.os == 'ubuntu-latest' && secrets.GPG_PRIVATE_KEY || '' }}
      GPG_PASSPHRASE: ${{ matrix.os == 'ubuntu-latest' && secrets.GPG_PASSPHRASE || '' }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.release_tag }}
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - name: Require a stable version
        shell: bash
        run: |
          node -e "const v=require('./package.json').version;const tag=process.env.RELEASE_TAG;if(v.includes('-'))throw new Error('Stable signing requires a version without a prerelease suffix: '+v);if(tag !== 'v'+v)throw new Error('Tag '+tag+' does not match package version v'+v)"
      - name: Require platform signing credentials
        shell: bash
        run: |
          case "$RUNNER_OS" in
            Linux)
              test -n "$GPG_PRIVATE_KEY"
              test -n "$GPG_PASSPHRASE"
              ;;
            macOS)
              test -n "$CSC_LINK"
              test -n "$CSC_KEY_PASSWORD"
              test -n "$APPLE_ID"
              test -n "$APPLE_APP_SPECIFIC_PASSWORD"
              test -n "$APPLE_TEAM_ID"
              ;;
            Windows)
              test -n "$CSC_LINK"
              test -n "$CSC_KEY_PASSWORD"
              ;;
          esac
      - run: npm ci
      - run: npm run typecheck
      - run: npm run lint
      - run: npm test
      - run: npm run test:abi
      - run: npm run benchmark:sqlite
      - name: Import Linux release key
        if: runner.os == 'Linux'
        shell: bash
        run: echo "$GPG_PRIVATE_KEY" | gpg --batch --import
      - run: npm run package -- --publish never
      - name: Smoke test signed package (Linux)
        if: runner.os == 'Linux'
        run: xvfb-run -a npm run smoke:package
      - name: Smoke test signed package
        if: runner.os != 'Linux'
        run: npm run smoke:package
      - run: npm run checksums
      - name: Sign Linux checksums
        if: runner.os == 'Linux'
        shell: bash
        run: |
          version="$(node -p "require('./package.json').version")"
          gpg --batch --yes --pinentry-mode loopback --passphrase "$GPG_PASSPHRASE" --detach-sign --output "release/$version/SHA256SUMS.sig" "release/$version/SHA256SUMS"
      - run: npm run verify:signatures
      - name: Preserve platform checksum manifest
        shell: bash
        run: |
          version="$(node -p "require('./package.json').version")"
          mv "release/$version/SHA256SUMS" "release/$version/SHA256SUMS-${{ runner.os }}"
          if [ -f "release/$version/SHA256SUMS.sig" ]; then mv "release/$version/SHA256SUMS.sig" "release/$version/SHA256SUMS-${{ runner.os }}.sig"; fi
      - uses: actions/upload-artifact@v7
        with:
          name: nocturne-signed-${{ runner.os }}-${{ runner.arch }}
          path: |
            release/*/*.AppImage
            release/*/*.tar.gz
            release/*/*.dmg
            release/*/*.exe
            release/*/*.zip
            release/*/*.blockmap
            release/*/*.yml
            release/*/SHA256SUMS-*
          if-no-files-found: error
          retention-days: 14

  stable-gate:
    name: Verify and publish stable release
    needs: signed-package
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write
    env:
      GH_TOKEN: ${{ github.token }}
      RELEASE_TAG: ${{ inputs.release_tag }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.release_tag }}
      - uses: actions/download-artifact@v8
        with:
          pattern: nocturne-signed-*
          path: release-assets
          merge-multiple: true
      - run: npm run verify:release-assets -- release-assets
      - name: Publish verified assets
        shell: bash
        run: |
          if gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
            gh release upload "$RELEASE_TAG" release-assets/* --clobber
          else
            gh release create "$RELEASE_TAG" release-assets/* --verify-tag --generate-notes --title "Nocturne Studio $RELEASE_TAG"
          fi
